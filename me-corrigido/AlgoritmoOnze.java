public class AlgoritmoOnze {
    public static void main() {

        // Entrada
        String nome = IO.readln("Digite o seu nome: ");
        String resultado = (nome.equalsIgnoreCase("Elias")) ? "Meu nome está correto" : "Meu nome está errado";
        // Verificação
        System.out.println(resultado);


    }
}
